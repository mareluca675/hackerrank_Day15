#include <iostream>
#include <string>

using namespace std;

int main()
{
    string S;
    getline(cin, S);

    try{
        cout << stoi(S);
    }catch (invalid_argument){
        cout << "Bad String";
    }
    

    return 0;
}
